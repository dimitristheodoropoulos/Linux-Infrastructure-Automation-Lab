replicaCount: 1

image:
  repository: dimitris424/ml-app
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 8000
  targetPort: 8000

ingress:
  enabled: true
  host: ml-app.local

resources: {}
